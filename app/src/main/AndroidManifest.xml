<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sebastiaan.sebastiaanjoustra_pset5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/todo_lists_title"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TodoListActivity"
            android:label="@string/title_activity_todo_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewListActivity"
            android:label="@string/title_activity_new_list"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".NewTodoItemActivity"
            android:label="@string/title_activity_new_todo_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TodoViewActivity"
            android:label="@string/title_activity_todo_view"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>